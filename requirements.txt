# Standard Python package requirements for the weather prediction ML pipeline.
# Pin exact versions or version ranges to ensure reproducibility across
# different environments. Additional optional packages such as Streamlit
# and matplotlib can be installed separately when needed for visualisation
# or dashboarding.

pandas>=3.0.0,<4.0.0
scikit-learn>=1.8.0,<2.0.0
requests>=2.32.5,<3.0.0
python-dotenv>=1.2.1,<2.0.0
joblib>=1.5.3,<2.0.0
tqdm>=4.67.1,<5.0.0
